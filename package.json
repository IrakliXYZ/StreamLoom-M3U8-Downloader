{
  "name": "streamloom",
  "version": "0.2.0",
  "description": "Build tooling for the StreamLoom Chrome extension.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "dev:watch": "node tools/build.mjs --watch"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.10",
    "@ffmpeg/util": "^0.12.2"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
